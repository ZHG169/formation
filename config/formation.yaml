# Split configuration entry point.
#
# The launch file no longer loads this file directly.  Parameters are
# split by node to keep the package readable:
#
# - mission.yaml
# - leader_command.yaml
# - leader_control.yaml
# - follower_control.yaml
# - distributed.yaml      # legacy distributed-mode parameters
#
# Start the normal leader/follower stack with:
#
#   ros2 launch formation formation.launch.py
